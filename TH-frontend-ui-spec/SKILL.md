---
name: frontend-ui-spec
description: 前端 UI 规范（按设计稿：字体配色/表格/表单/弹框等）。当用户输入"UI规范"、"前端UI规范"、"前端ui规范"、"frontend-ui-spec"、"frontend ui spec"时触发。
allowed-tools: Read, Grep, Glob
---

# 前端 UI 规范

按设计稿实现与验收前端页面时，逐条对照以下规范。

## 字体，配色

- 字体颜色与字号严格按照设计稿走。配色方案，按设计稿的设计规范来。

## 表格（table）

- 按照设计稿，操作栏的icon button ,padding一致，icon按钮需要有tooltip提示文字，删除icon的颜色一致，删除弹框一致，统一是popconfirm，批量操作删除再用confirm 的modal
- 操作栏按钮、不能点击的状态是置灰，不要消失哈
- 表格空数据的时候，显示‘-’
- 注意表格列的宽度，根据字段长度设置相应合理的宽度，尽量避免出现没必要的水平滚动条。
- 字符超长显示...,鼠标滑过显示全部，不用tooltip，用默认样式
- 根据设计稿，整个项目表格列的对齐方式一致。
- 钱数，一般右对齐，3位一个逗号
- 表格如果是1页的话，就不要显示页码了
- 表格的表头文字的提示tooltip，叹号icon要和文字水平方向上居中对齐
- loading、空状态的样式的一致
- 表格的中的tag，如果字符数量差不多，tag就定个宽，让不同的tag宽度一致，注意，ant的tag有个小bug，文字没有垂直居中对齐。如果字符数量差别很大，就用点加文字的形式。注意，项目里tag的配色主题一致。

## 页面标题（pagetitle）

- 根据设计稿统一，字体的大小，颜色，尤其是行距，上下的padding。一般是左对齐，注意默认的h标签有padding和margin注意清空一下。

## 搜索栏

- 多个搜索条件，一般是宽度一致，按钮组右对齐，注意主按钮在右，次按钮在左，注意顶格。

## 页面板块

- 根据设计稿，注意板块的内外边距，一般是20px或16px或10px或12px。一般一个项目只会出现一个值，按设计稿来。
- 注意卡片的圆角与阴影，注意和设计稿一致
- card 的版块，详情页，信息加载应该有骨架屏的占位loading

## 表单项

- 根据设计稿，表单元素的一致性，包括按钮，vben或者ant design，二选一
- 表单项每一个的行距一致，上下间距一致。
- 表单的中的 placeholder写有用的信息，比如输入的规则是什么。
- 表单域如果有默认值就设置一个合理的默认值，比如，checkbox，radio，switch、slider等
- 表单label现在基本上都是右对齐，但是也要注意label宽度的合理性。
- 表单提交后要有相应的相关message信息。

## 弹框

- 根据设计稿，弹框用一种库 。
- 弹框中的表单，根据弹框的padding撑满弹框的区域，表单超长，弹框里加滚动条，不要整个屏幕滚。
- 整个项目弹框弹出的位置统一，居上就都居上，居中就都居中。
- 关闭弹窗的方式统一，有的是关闭空白区域能关，有的是不能关，咱们都统一了。
- 弹出的title，注意标题文字的一致性。不要有的是编辑，有的是更新

## 一些小问题

- 图标：线性、面性图标的一致性，切svg格式，图标和文字，注意水平方向上对齐，图标和文字额距离一般是3px
- 动态加载的数据要加loading
- 链接文字的主题色一致
- 注意自适应的问题
- 页面一屏可以展示开的尽量不要出现竖向的滚动条 ，有搜索条件的就搜索条件的下方的区域滚动。
- 统一项目中出现所有的虚线，样式一致。
- 注意可以点击区域的是鼠标的cursor：pointer，
- 整个项目缺省图的一致。缺省图下的提示文字，用可编辑文字，不可以使用图片。
